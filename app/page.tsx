import { MetricsView } from "@/components/metrics-view"
//all this code just to render the component showing the temp gauge, ph bar, power buttonm and cycle count.

export default function MetricsPage() {
  return <MetricsView />
}
