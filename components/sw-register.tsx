"use client"

// Service worker registration is handled by next-pwa (register: true in next.config.js).
// This component is kept as a no-op to avoid breaking existing imports.
export function ServiceWorkerRegister() {
  return null
}
